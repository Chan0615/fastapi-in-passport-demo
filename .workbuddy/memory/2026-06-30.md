# 2026-06-30

- 搭建 FastAPI + Ant Design + Docker 前后端分离开发模板
  - 后端：FastAPI + Python 3.12 + SQLAlchemy 2.0 + Pydantic Settings，数据库连接串全部走环境变量
  - 前端：React 18 + Ant Design 5 + Vite + TypeScript + Axios
  - 部署：docker-compose 编排 MySQL + 后端 Uvicorn + 前端 Nginx
  - 目录结构：backend/app 按 api/core/models/schemas/services 分层；frontend/src 按 layouts/pages/services/utils 分层
  - 改造模板为“配置中心数据库化”：
  - 项目内仅保留 backend/bootstrap.yaml，只存主数据库地址、app_env、可选 encrypt_key。
  - 新增 sys_config 表，所有基础设施配置（Redis/Mongo/外部 API/密钥/CORS/功能开关）全部走数据库读取。
  - 新增 ConfigManager 服务：启动加载到内存缓存、支持加密字段解密、提供 /api/v1/config/reload 热重载。
  - 新增 backend/scripts/init_config.sql 初始化示例数据与表结构。
  - 更新 docker-compose.yml：新增 Redis/Mongo 服务，后端只挂载 bootstrap.yaml。
  - 更新 README.md、skill 资产与 fastapi-antd-template.skill 包。
